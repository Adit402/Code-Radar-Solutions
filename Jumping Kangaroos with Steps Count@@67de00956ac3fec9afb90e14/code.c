int kangaroo(int a, int  b, int c, int d){
    int i,j;
    for(i=0;i<1(000;i++){
        if((a + (b*i)) == (c + (d*i))){
            return i;
            return 0;
        }
    }
    return -1;
}